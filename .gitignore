*.iml
.gradle
/local.properties
/.idea
.DS_Store
**/build/
/captures
.externalNativeBuild
.cxx
local.properties
app/build
/app/release
.kotlin

# mkdocs local-build output and the venv used to build it
/site/
.venv-docs/

# Release signing keystores — NEVER commit
*.keystore
*.jks

# Claude Code agent worktrees
/.claude/

# Host-JVM native libs dropped by ObjectBox when running unit tests on Mac/Windows.
# (Android .so files under app/src/main/jniLibs/ are intentionally tracked.)
*.dylib
*.dll

# Local scratch notes / deferred-audit artefacts
/.context/
